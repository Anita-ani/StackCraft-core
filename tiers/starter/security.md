# Starter — security

- TLS for users; firewall minimal exposure (80/443; SSH from known IPs if used).  
- SSH keys; disable password auth.  
- Patch strategy (unattended upgrades or managed).  
- Secrets in manager—not git.

High **PRI**: sector baselines may require more—[stackcraft-sectors](../../../stackcraft-sectors/).
